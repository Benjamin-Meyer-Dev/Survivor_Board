{
  "name": "survivor-board",
  "version": "1.0.0",
  "private": true,
  "description": "Shared college and NFL survivor pool tracker. Static site, no build step.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "serve": "npx --yes serve . -l 4173",
    "refresh": "node scripts/refresh-odds.mjs",
    "seed": "node scripts/seed-plan.mjs",
    "passcode": "node scripts/set-passcode.mjs",
    "icons": "node scripts/build-icons.mjs",
    "test": "node scripts/validate-plan.mjs && node scripts/validate-board-state.mjs && node scripts/validate-store-sync.mjs && node scripts/validate-config.mjs",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build:artifact": "node scripts/build-artifact.mjs"
  },
  "devDependencies": {
    "eslint": "^9.12.0",
    "prettier": "^3.3.3"
  }
}
